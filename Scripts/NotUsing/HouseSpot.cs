using UnityEngine;
using System.Collections;

public class HouseSpot : MonoBehaviour {

    void OnMouseUp() { /*
        BuildingManager bm = GameObject.FindObjectOfType<BuildingManager>();
        if (bm.buildingSpots != null) {
            Instantiate(bm.buildingSpots, transform.parent.)
        }     */
    }
}
